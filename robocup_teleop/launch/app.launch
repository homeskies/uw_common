<?xml version="1.0"?>
<launch>
	<include file="$(find uw_gazebo)/launch/house_simulation.launch" />


	<!-- launch rviz with config/access_teleop_config.rviz -->
	<!-- <node pkg="rviz" type="rviz" name="rviz"
      args="-d $(find access_teleop)/config/access_teleop_config.rviz"
      if="$(arg use_rviz)" /> -->

	<!-- launch the point cloud saver to allow point cloud freezing -->
	<node pkg="robocup_teleop" output="screen" name="pointcloud_saver" type="robocup_pointcloud_pub.py" />
  
	<!-- launch the backend -->
	<node pkg="robocup_teleop" output="screen" name="robocup_teleop" type="robocup_teleop.py" />
</launch>
